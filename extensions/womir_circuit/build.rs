fn main() {
    // CUDA kernel compilation is disabled for now.
    // Currently only halt() is supported on GPU, which is handled by OpenVM's system.
    // When WOMIR-specific GPU tracegen is needed, re-enable kernel compilation here.
}
